= render '/shared/page_header', header: t('plurals.alert')

= render 'alerts', alerts: @alerts

- if @current_user.try(:admin)
	%div= link_to t(:new_alert), new_alert_path, class: get_btn_class(:create)
