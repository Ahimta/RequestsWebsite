- header = t('.header')
= provide :title, header

%div.page-header
	%h1= header

= render 'applicant', applicant: @applicant
%hr

%h2 Requests
= render '/requests/requests', requests: @applicant.requests
