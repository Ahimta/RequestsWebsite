= provide :title, action

= form_for @leave do |f|
	= render '/requests/form', f: f, type: Leave::Type, action: action
	
	%div= f.text_field :reason, placeholder: t('.reason')
	
	= render '/shared/form_footer', f: f, action: action
