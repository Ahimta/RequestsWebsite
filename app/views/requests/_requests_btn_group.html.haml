%div.btn-group{ dir: 'ltr' }
	= link_to(t(:accept),
		new_request_decision_path(request, d: 'accept'),
		class: get_btn_class(:accept))

	= link_to(t(:reject),
		new_request_decision_path(request, d: 'reject'),
		class: get_btn_class(:reject))
