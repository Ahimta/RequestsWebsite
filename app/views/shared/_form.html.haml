%div.page-header
	%h1= action
= f.fields_for :request do |h|
	= h.fields_for :applicant do |i|
		= i.hidden_field :user_id, value: 0
		- applicant = t(:applicant)
		%div= i.text_field :name, placeholder: applicant, autofocus: ''