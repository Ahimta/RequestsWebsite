- options ||= {}

%div
	= f.text_field(symbol, placeholder: text, title: text,
			autofocus: options[:autofocus], required: !options[:extra],
			class: options[:klass])
	
	= get_required_label(!options[:extra])
