- if items.empty?
	%p.lead= empty

- else
	%table.table.table-striped
		%thead
			%tr
				%th #
				- columns.each do |column|
					%th= column
		%tbody
			- i = 1
			- items.each do |item|
				%tr
				%td= i
				- i += 1
				- cells.each do |cell|
					%td= cell
