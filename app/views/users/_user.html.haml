= render('/shared/resource_attributes',
	attributes: { t(:username) => user.username,
	t(:location) => link_to(user.location_name, user.location),
	t(:applicants_count) => user.applicants.size,
	t(:requests_count) => user.requests.size })
