- users = t('.users')
= provide :title, users

%div.page-header
	%h1= users

= render 'users', users: @users, link_location: true

= link_to t('users.new.action'), new_user_path, class: 'btn btn-large btn-success'
