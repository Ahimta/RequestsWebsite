- header = t('.header') + " - #{@user.username}"
= provide :title, header

%div.page-header
	%h1= header

= render 'user', user: @user
%hr
= render '/applicants/applicants', applicants: @user.applicants
